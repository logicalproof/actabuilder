<%= form_for(@assigned_ship) do |f| %>
  <% if @assigned_ship.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@assigned_ship.errors.count, "error") %> prohibited this assigned_ship from being saved:</h2>

      <ul>
      <% @assigned_ship.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :ship_id %><br />
    <%= f.number_field :ship_id %>
  </div>
  <div class="field">
    <%= f.label :fleet_list_id %><br />
    <%= f.number_field :fleet_list_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
